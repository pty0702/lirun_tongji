# 利润统计工具 — 交付文档

## 1. 项目概述

**利润统计工具**是一个本地桌面应用程序，用于读取电商订单表和成本表，按商品ID和商家编码自动计算利润，并生成格式化的 Excel 统计报告。

- **开发语言**：Python 3
- **GUI 框架**：Tkinter（Python 标准库，无需额外安装）
- **数据处理**：pandas + openpyxl
- **运行环境**：Windows / macOS / Linux

---

## 2. 环境准备

### 2.1 安装 Python

确保系统已安装 Python 3.7 或更高版本。在终端中验证：

```bash
python --version
```

如果未安装，请从 [python.org](https://www.python.org/downloads/) 下载安装。

### 2.2 安装依赖

打开终端，进入项目目录，执行：

```bash
cd D:\python\lirun_tongji
pip install -r requirements.txt
```

依赖包清单（`requirements.txt`）：

| 包名 | 最低版本 | 用途 |
|------|---------|------|
| pandas | ≥1.3.0 | 读取和清洗 Excel 数据 |
| openpyxl | ≥3.0.0 | 生成带格式的输出 Excel |

> Tkinter 是 Python 标准库的一部分，无需额外安装。

---

## 3. 项目文件结构

```
项目根目录/
├── main.py                  # GUI 主程序入口（tkinter 界面）
├── profit_core.py           # 核心计算逻辑模块 + 命令行入口
├── requirements.txt         # Python 依赖清单
├── README.md                # 项目简要说明
├── 交付文档.md               # 本文件（详细交付文档）
├── 咔咔唛.xlsx              # 订单表示例文件
├── 24日成本表.xlsx           # 成本表示例文件
└── output/                  # 输出目录（程序自动创建）
```

---

## 4. 启动方式

### 4.1 GUI 模式（推荐）

双击 `main.py` 或在终端中运行：

```bash
python main.py
```

启动后弹出图形界面窗口，通过按钮选择文件即可完成全部操作。

### 4.2 命令行模式

适用于脚本自动化或批处理场景：

```bash
# 统计全部数据
python profit_core.py

# 指定日期范围
python profit_core.py 2026-05-11 2026-05-24
```

命令行模式默认读取项目目录下的 `咔咔唛.xlsx`（订单表）和 `24日成本表.xlsx`（成本表），输出到 `output/`。

---

## 5. GUI 界面说明

```
┌──────────────────────────────────────────────────┐
│  利润统计工具 v2.0                               │
├──────────────────────────────────────────────────┤
│  订单表：  [__________________] [选择文件]       │
│  成本表：  [__________________] [选择文件]       │
│  输出目录：[__________________] [选择目录]       │
│  ─────────────────────────────────────           │
│  日期筛选（可选）：                              │
│  开始日期：[________] YYYY-MM-DD                 │
│  结束日期：[________] YYYY-MM-DD                 │
│  ─────────────────────────────────────           │
│  [▶ 开始计算] [📂 打开输出文件] [清空日志]       │
│  ┌──────────────────────────────────┐            │
│  │ 📊 统计摘要                      │            │
│  │ 订单表行数  成本表行数  ...      │            │
│  └──────────────────────────────────┘            │
│  运行日志：                                      │
│  ┌──────────────────────────────────┐            │
│  │ 欢迎使用利润统计工具...          │            │
│  │ [1/5] 读取成本表...              │            │
│  │ ...                              │            │
│  └──────────────────────────────────┘            │
│  就绪                                            │
└──────────────────────────────────────────────────┘
```

### 5.1 功能介绍

| 功能区域 | 说明 |
|---------|------|
| **自动检测文件** | 启动时自动扫描目录，识别订单表和成本表并预填路径 |
| **文件选择** | 点击按钮弹出系统文件对话框，支持 xlsx/xls 格式 |
| **输出目录** | 选择 Excel 报告的输出位置，默认为 `./output/` |
| **日期筛选** | 可选，按订单提交时间过滤数据，格式为 YYYY-MM-DD |
| **开始计算** | 点击后后台异步执行，不阻塞界面 |
| **打开输出文件** | 计算成功后启用，一键打开生成的 Excel 文件 |
| **清空日志** | 清除运行日志面板内容 |
| **统计摘要** | 计算完成后显示订单行数、成本行数等关键指标 |
| **状态栏** | 底部显示当前操作状态 |

---

## 6. 操作流程

### 步骤一：准备输入文件

准备两个 Excel 文件：

1. **订单表** — 包含所有订单明细
2. **成本表** — 包含各商品/编码的成本单价

### 步骤二：启动程序

```bash
python main.py
```

程序启动后会自动检测当前目录下的 Excel 文件并预填路径。

### 步骤三：确认文件路径

- 如果自动检测正确，无需操作
- 如需更换，点击"选择文件"按钮手动选取
- 确认输出目录（默认 `./output/`）

### 步骤四：（可选）设置日期范围

如果只需要统计某个时间段的数据，填写开始日期和结束日期。
不填则统计全部数据。

### 步骤五：开始计算

点击 **「▶ 开始计算」** 按钮，程序将在后台执行计算，
日志面板实时显示进度。计算完成后弹出结果提示。

### 步骤六：查看结果

- 点击 **「📂 打开输出文件」** 直接打开生成的 Excel
- 或在输出目录中手动打开

---

## 7. 输入文件格式要求

### 7.1 订单表

必须为 `.xlsx` 或 `.xls` 格式，第一行为表头。

**必需字段**（支持模糊匹配，不区分大小写）：

| 字段 | 别名（任一即可） | 说明 |
|------|-----------------|------|
| **商品ID** | 商品ID、商品id、产品ID、货品ID、商品编号、商品编码 | 商品的唯一标识 |
| **商家编码** | 商家编码、商品编码、SKU编码、SKU、规格编码、商品条码 | 与成本表匹配的编码 |
| **商品数量** | 商品数量、数量、购买数量、件数、下单数量 | 下单件数 |
| **订单提交时间** | 订单提交时间、提交时间、下单时间、创建时间、订单时间 | 用于日期筛选 |
| **商家收入** | 商家收入金额、商家收入、商家实收、结算金额、实收金额、收入 | 该行订单的收入金额 |
| **订单号**（可选） | 主订单编号、子订单编号、订单号、订单编号 | 用于异常明细追溯 |

### 7.2 成本表

必须为 `.xlsx` 或 `.xls` 格式，第一行为表头。

**必需字段**（支持模糊匹配）：

| 字段 | 别名（任一即可） | 说明 |
|------|-----------------|------|
| **编码列** | 商品编码、商品ID、商家编码、编码、名称、商品名称、品名、产品编码、货号、SKU 等 | 与订单表商家编码匹配 |
| **成本列** | 总计成本、成本、成本价、成本单价、单价、价格、金额、进货价、采购价 等 | 单个商品的成本单价 |

---

## 8. 输出文件说明

生成的 Excel 文件包含 **4 个 Sheet**：

### Sheet 1 — 商品ID编码明细

按「商品ID + 商家编码」粒度汇总的利润明细表。

| 列名 | 说明 |
|------|------|
| 商品ID | 商品唯一标识 |
| 商家编码 | 商品对应的商家编码/SKU |
| 成本单价 | 从成本表匹配到的成本单价 |
| 编码对应订单行数 | 该编码对应的订单行数 |
| 编码对应商品数量 | 该编码的商品数量合计 |
| 编码对应总成本 | 成本单价 × 商品数量 |
| 编码对应总收入 | 该编码的总收入 |
| 编码对应总利润 | 总收入 - 总成本 |
| ID对应总利润 | 同一商品ID下所有编码的总利润 |
| 状态 | 始终为「正常」 |

### Sheet 2 — 商品ID汇总

按「商品ID」粒度的汇总表。

| 列名 | 说明 |
|------|------|
| 商品ID | 商品唯一标识 |
| 订单行数 | 该商品的总订单行数 |
| 商品数量合计 | 该商品的总销售数量 |
| 总收入 | 该商品的总收入 |
| 总成本 | 该商品的总成本 |
| 总利润 | 总收入 - 总成本 |
| 异常编码数量 | 该商品下存在异常的编码种类数 |
| 异常订单行数 | 该商品下异常的订单行数 |

### Sheet 3 — 异常明细

不参与利润计算的异常数据明细（红色表头）。

| 列名 | 说明 |
|------|------|
| 异常类型 | 商家编码为空 / 商品数量异常 / 商家收入异常 / 成本缺失 |
| 商品ID | 原订单中的商品ID |
| 商家编码 | 原订单中的商家编码 |
| 商品数量 | 原订单中的商品数量 |
| 商家收入 | 原订单中的商家收入 |
| 订单提交时间 | 原订单中的提交时间 |
| 订单号 | 原订单中的订单号 |
| 原因 | 具体的异常原因描述 |

### Sheet 4 — 成本表快照

成本表的完整处理结果，包含每行数据的状态。

| 列名 | 说明 |
|------|------|
| 商家编码 | 成本表中的编码 |
| 成本单价 | 成本表中的成本单价 |
| 状态 | 正常 / 成本重复 / 编码为空 / 成本异常 |
| 原因 | 异常原因说明 |

---

## 9. 异常处理说明

以下情况的数据会被标记为**异常**，不参与利润计算：

| 异常类型 | 触发条件 | 处理方式 |
|---------|---------|---------|
| **商家编码为空** | 订单表中商家编码为空、或为无效值（`-`、`--`、`nan`、`None`、`无` 等） | 记入异常明细 |
| **商品数量异常** | 商品数量为空、无法转为数字、或 ≤ 0 | 记入异常明细 |
| **商家收入异常** | 商家收入为空或无法转为数字 | 记入异常明细 |
| **成本缺失** | 订单表中的商家编码在成本表中找不到匹配 | 记入异常明细 |
| **成本重复** | 成本表中同一个编码对应多个不同成本金额 | 该编码的所有相关行均不参与计算 |

### 数据清洗规则

程序自动处理以下格式问题：

- 字符串首尾空格自动去除
- 制表符 `\t`、换行符 `\n`、回车符 `\r` 自动清除
- 空值标记（`-`、`--`、`nan`、`NaN`、`None`、`NULL`、`无`）统一识别
- 金额字段中的逗号（`,`、`，`）和人民币符号（`￥`、`¥`）自动去除
- 大整数自动保留原始格式，防止科学计数法
- 日期字段支持多种常见格式自动解析

---

## 10. 常见问题（FAQ）

### Q1：启动报错 "No module named 'pandas'"

**原因**：依赖未安装。

**解决**：运行 `pip install -r requirements.txt`

### Q2：提示"订单表缺少必要字段"

**原因**：订单表的表头列名不在程序的识别范围内。

**解决**：
- 检查表头是否在第一行
- 确保表头包含：商品ID、商家编码、商品数量、订单提交时间、商家收入（或它们的别名）
- 别名列表参见第 7 节

### Q3：提示"无法识别成本表编码列/成本列"

**原因**：成本表表头列名无法匹配。

**解决**：
- 确保成本表包含编码列和成本列
- 别名列表参见第 7.2 节

### Q4：部分数据进入了异常明细

这是正常的。程序会严格校验数据质量，以下情况会进入异常：
- 商家编码为空/无效
- 商品数量 ≤ 0
- 收入为空
- 成本表中找不到对应编码

请在异常明细 Sheet 中查看具体原因并进行数据修正。

### Q5：成本表中同一编码有多个成本价

程序将此情况标记为「成本重复」，该编码对应的所有订单行都不会参与计算。
请在成本表中统一每个编码的成本价（只保留一条正确的记录）。

### Q6：输出文件在哪里？

默认在项目目录的 `output/` 子目录下，文件名格式：
- 有日期筛选：`利润统计结果_2026-05-11_2026-05-24.xlsx`
- 无日期筛选：`利润统计结果_全部数据.xlsx`

也可以在 GUI 中自定义输出目录。

### Q7：可以在没有 Python 环境的电脑上运行吗？

需要先安装 Python。如果需要在无 Python 环境的电脑上运行，可以使用 PyInstaller 打包成 exe。

打包命令（可选）：

```bash
pip install pyinstaller
pyinstaller --onefile --windowed --name="利润统计工具" main.py
```

打包后的 exe 文件在 `dist/` 目录下，可直接分发给用户使用（无需安装 Python）。

---

## 11. 技术架构

```
┌─────────────────────────────────────────┐
│              main.py (GUI)              │
│  Tkinter 界面层                         │
│  · 文件选择对话框                       │
│  · 日期输入                             │
│  · 异步执行（threading）                │
│  · 实时日志输出                         │
│  · 统计摘要展示                         │
└──────────────┬──────────────────────────┘
               │ import
┌──────────────▼──────────────────────────┐
│         profit_core.py (核心)           │
│  · 字段自动识别（模糊匹配）             │
│  · 数据清洗（空值、格式、类型转换）     │
│  · 成本表处理（编码去重、异常检测）     │
│  · 利润计算（成本匹配、汇总聚合）       │
│  · Excel 输出（4 Sheet、格式化）        │
└──────────────┬──────────────────────────┘
               │ 依赖
┌──────────────▼──────────────────────────┐
│   pandas  (数据读取与清洗)              │
│   openpyxl (Excel 写入与格式化)         │
└─────────────────────────────────────────┘
```

### 设计要点

| 要点 | 说明 |
|------|------|
| **字段模糊匹配** | 表头列名支持多种别名，适应不同数据源的命名习惯 |
| **分离关注点** | GUI 层（main.py）与业务逻辑层（profit_core.py）完全分离，命令行和 GUI 共用同一核心 |
| **异步计算** | GUI 使用独立线程执行计算，界面不会卡顿 |
| **防御性清洗** | 对所有输入数据进行严格清洗和校验，脏数据进入异常明细而非导致崩溃 |
| **可追溯性** | 异常明细和成本表快照提供完整的数据溯源 |

---

## 12. 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0 | 2026-05 | 初始版本：核心计算逻辑 + 基础 GUI |
| v2.0 | 2026-05 | GUI 增强：自动检测文件、打开输出按钮、统计摘要面板、状态栏、改进的 UI 布局 |

---

## 13. 联系与支持

如遇到问题或需要功能定制，请提供以下信息以便排查：

1. 操作系统版本
2. Python 版本（`python --version`）
3. 错误日志截图或文本
4. 输入文件表头截图（前 2 行即可）
